class Common {
    static alert(msg) {
        alert(msg);
    }

    static confirm(msg) {
        return confirm(msg);
    }

    static prompt(msg, defaultValue) {
        return prompt(msg, defaultValue);
    }
}